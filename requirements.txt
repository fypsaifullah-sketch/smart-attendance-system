streamlit==1.38.0
numpy==1.26.4
pandas==2.2.2
openpyxl==3.1.5
matplotlib==3.9.2
seaborn==0.13.2
plotly==5.24.1
bcrypt==4.2.0
fpdf2==2.7.9
Pillow==10.4.0
opencv-python-headless==4.10.0.84

# --- Face recognition stack ---
# dlib-bin ships precompiled wheels (Linux/Mac/Windows) so Streamlit Cloud's
# build step doesn't have to compile dlib from source (that step is the
# #1 reason these deployments fail or time out). face_recognition then
# picks up this "dlib" install automatically.
dlib-bin==19.24.6
face_recognition==1.3.0
face-recognition-models==0.3.0
